<% if @is_bolsao %>
  <%= title  'Novo Candidato',
    link_to('Voltar', students_path(is_bolsao: @is_bolsao)) %>
  <%= render 'form_bolsao' %>
<% else %>
  <%= title 'Novo Aluno',
    link_to('Voltar', students_path(is_bolsao: @is_bolsao)) %>
  <%= render 'form' %>
<% end %>