<%= title 'Product Types', link_to('New Product Type', new_product_type_path) %>

<table id='product_types' class='display is-datatable'>
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
  <% @product_types.each do |product_type| %>
    <tr>
      <td>
        <%= product_type.name %> 
        <%= link_to 'Edit', edit_product_type_path(product_type), class: 'float-right' %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>