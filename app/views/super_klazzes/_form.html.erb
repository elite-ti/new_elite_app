<%= simple_form_for(@super_klazz) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :product_year %>
    <%= f.association :campus %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
