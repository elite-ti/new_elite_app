<%= simple_form_for(@product) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: 'Nome' %>
    <%= f.association :product_type, label: 'Segmento' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
