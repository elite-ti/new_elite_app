<%= title 'New Student Exam', link_to('Back', root_path) %>

<%= simple_form_for(@student_exam, html: { multipart: true }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :card %>
    <%= f.association :exam, input_html: { class: 'is-chosen' } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
