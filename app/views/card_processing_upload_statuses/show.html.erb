<%= title 'Status de Envio de Cartões',
  link_to('Back', card_processing_upload_statuses_path) %>

<%= form_tag card_processing_upload_statuses_path(group_by: 'super_klazz'), :method => 'get' do %>
  <p>
    <%= select_tag :exam_date, 
      options_for_select(@possible_dates, @exam_date)
    %>
    <%= hidden_field_tag :is_bolsao, true %>
    <%= submit_tag "Filtrar", :name => nil %>
  </p>
<% end %>

<table id="card_processing_upload_status"  class='display is-datatable'>
  <thead>
    <tr>
      <% @titles.each do |title| %>
        <th><%= title %></th>
      <% end %>
    </tr>
  </thead>

  <tbody>
  <% @statuses.each do |status| %>
    <tr>
      <% status.each do |field| %>
        <td><%= field %></td>
      <% end %>
    </tr>
  <% end %>
  </tbody>
</table>