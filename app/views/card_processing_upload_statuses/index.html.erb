<%= title 'Resultado por dia',
  link_to('Voltar', card_processing_upload_statuses_path) %>

<table id="card_processing_upload_statuses" class="display is-datatable">
  <thead>
    <tr>
      <th>Dia</th>
      <th>Ações</th>
    </tr>
  </thead>

  <tbody>
  <% @possible_dates.each do |date| %>
    <tr>
      <td><%= date.strftime('%Y-%m-%d') %></td>
      <td><%= link_to('Download Resultado', card_processing_upload_status_path(date, 'csv')) %></td>
    </tr>
  <% end %>
  </tbody>
</table>