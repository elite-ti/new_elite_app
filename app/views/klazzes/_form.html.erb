<%= simple_form_for(@klazz) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :super_klazz, 
      label_method: :label_method %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
