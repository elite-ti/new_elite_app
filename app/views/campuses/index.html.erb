<%= title 'Unidades', 
  (link_to('Nova Unidade', new_campus_path) if can?(:create, Campus)) %>

<table id='campuses' class='display is-datatable'>
  <thead>
    <tr>
      <th>Nome</th>
    </tr>
  </thead>

  <tbody>
    <% @campuses.each do |campus| %>
      <tr>
        <td>
          <%= campus.name %>
          <span>
            <%= link_to('Mostrar', campus) %> 
            <% if can?(:update, campus) %>
              | <%= link_to('Editar', edit_campus_path(campus)) %>
            <% end %>
          </span>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>